// src/models/Course.js
class Course {
    constructor(id, name, holes) {
        this.id = id;
        this.name = name;
        this.holes = holes; // Array of hole pars
    }
}

export default Course;